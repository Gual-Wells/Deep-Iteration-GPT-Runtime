# Only B hard

`DIGR(B=1)：验证边界条件`

This is valid adaptive input. After the pinned execution protocol is loaded, native completion selects a task-proportionate positive T; B=1 freezes it as a hard minimum. `min=10min` remains the explicit-value alternative.
