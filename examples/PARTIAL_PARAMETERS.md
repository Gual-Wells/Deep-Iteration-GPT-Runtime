# Partial parameter completion

`DIGR(N=5,D(1),V(2),L(2))：任务` freezes explicit N/D/V/L and leaves T/R/S for one task-aware native completion. Completion cannot shift or lower explicit values.

`DIGR(profile=standard,D(1),V(2),L(2))：任务` instead overlays D/V/L on the explicit fixed standard profile.
