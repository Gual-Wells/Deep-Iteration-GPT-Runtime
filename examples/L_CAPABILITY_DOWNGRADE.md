# L capability honesty

Requested:

`DIGR（D（1），L（3））：...`

If the host only has one shared ChatGPT context, it must not call that L3 merely because the prompt names a Dictator role. The highest supported mode is L1. Best-effort work may continue, but proof must expose the mismatch, e.g. `L（3）/L（1）` (or `?` when actual capability is genuinely unknown).
