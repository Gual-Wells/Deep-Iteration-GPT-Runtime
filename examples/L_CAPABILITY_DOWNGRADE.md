# L target, capability and actual

Requested: `DIGR（D（1），L（3））：...`

If the host can only implement same-context semantic separation, record `L_target=3`, `L_cap=1`, and execute only `L_actual=1`. Do not report L3 merely because the prompt names an agent or because a host supports some unrelated multi-agent feature.

Best-effort work may continue with visible mismatch such as `L（3）/L（1）`. It becomes a delivery blocker only when U0 separately made exact L achievement a hard condition. Each counted D intervention must reference the isolation receipt that proves the actual mode it used.
