# Berta2 Run Genesis

1. local capture acquires current stable, pins manifest/VERSION and loads STARTUP;
2. pinned STARTUP classifies the exact message and structurally parses parameters;
3. EXECUTING verifies descriptor/execution bundle before publishing staged Clock Genesis;
4. the pinned protocol is bound and native intelligence fills missing task-scale values once;
5. the completed receipt, exact U0 and Effective Contract freeze in order;
6. MAIN begins with Strategy; Candidate waits for meaningful output.

NATIVE/HELP/INVALID/AMBIGUOUS create no run. Missing host attestation does not block MODEL_NATIVE execution. Repository/descriptor/bundle failure before Genesis creates no published run.
