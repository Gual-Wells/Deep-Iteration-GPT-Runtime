# L2 handoff non-conformance

`DIGR（D（1），L（2））：任务`

“把 Main 的完整历史原样 handoff 给另一个 agent”不满足 L2，因为 D 已继承完整 Main context。L2 要求 separate context + controlled/compressed telemetry；D 只获得形成独立判断所需的受控高层状态，coup 前 D-State 不回流 Main。
