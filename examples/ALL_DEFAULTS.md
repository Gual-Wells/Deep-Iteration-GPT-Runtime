# Adaptive default and explicit standard

`DIGR：任务` is adaptive: structural parsing defaults B=0, b=0, L1 and source-required, while native intelligence completes missing N/T/R/S/D/V from U0 and task scale.

`DIGR(standard)：任务` explicitly selects fixed N2/T0/R1/S0/D0/V0/L1/source-auto. Actual quality-driven work may exceed zero/minimum values.
