# Only source b hard

`DIGR(S(b=1))：运行来源测试`

This is valid adaptive input. Native completion selects n/t/r and must choose positive source t because b=1. Explicit `S(t=5min,b=1)` remains valid when the user wants the value fixed.
