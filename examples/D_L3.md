# D with L3 — agent-isolated intervention

Invocation: `DIGR（D（2），L（3））：工程任务`

L3 includes the L2 controlled Input Packet → isolated work → Output Packet boundary and additionally requires independently evidenced agent identity, instructions, execution loop and tool lifecycle. Merely naming a “Dictator Agent” in the same context is not L3.

Each completed intervention references its actual isolation receipt. If the host cannot provide L3, it must expose the lower `L_actual` rather than turn host maximum capability or a role label into a false L3 claim.
