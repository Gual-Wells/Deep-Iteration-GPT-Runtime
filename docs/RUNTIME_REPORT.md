# Public Runtime Report

运行记录的目标是让用户看到“任务定义怎样被优化、执行链怎样设计、是否发生结构性重做”，而不是让模型证明自己认真思考过。

推荐字段：参数、提示词迭代、P*、进化度量、重做、实际流程链、查询/引用来源。

P* 必须是可公开的用户级执行提示词。流程链只能列高层真实动作，例如“读取仓库→检查 schema→生成文件→运行测试”，不能转录隐藏推理。

评分采用 0–5 整数，是运行时自评；禁止伪精确百分比和故意压低基线。
