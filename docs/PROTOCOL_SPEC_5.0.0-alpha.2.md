# DIGR 5.0.0-alpha.2 Protocol Specification Index

Normative semantics are the pinned `entry/DEEP_ITERATION_ENTRY.md` plus manifest `core[]`. Minimal pre-full-load behavior is the manifest `startup_slice`. `entry/HELP.md` is user-facing documentation and does not itself start a task run.

This Alpha 2 specification is the corrected integration baseline: immutable commitments plus perpetually revisable strategy; source presumption; candidate-backed R; integrated D/L information flow; comprehensive state/recovery; event/source/clock binding; compact proof.
