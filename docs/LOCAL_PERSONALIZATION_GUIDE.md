# Local Personalization Guide

Recommended file:
`local-personalization/CHATGPT_LOCAL_PERSONALIZATION.txt`

Purpose:
- make explicit DIGR calls activate 3.0 behavior;
- preserve strict non-sticky semantics;
- optionally resolve the GitHub `stable` protocol to one commit per run;
- provide an embedded 3.0 fallback if remote read fails or still exposes legacy 2.x semantics.

The personalization text is intentionally compact. Full behavioral detail remains in the repository core.
