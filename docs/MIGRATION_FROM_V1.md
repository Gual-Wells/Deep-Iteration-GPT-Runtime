# Migration from digr-v1

## 主要变化

- `digr-v1` 双目标两阶段升级为 `digr-v2` 三目标三阶段。
- 新增原生能力保全。
- 新增资源与工作流进化。
- 新增结果回代与六类审计。
- 新增工作包与结果审计 Schema。

## 不兼容点

- `manifest.json` 结构变化；
- 核心文件顺序变化；
- 运行状态增加 stage；
- 本地个性化规则必须更新；
- v1 模块不能被视为已满足 v2 的资源与结果回代要求。

建议保留 v1 tag，不原地覆盖稳定版本。
