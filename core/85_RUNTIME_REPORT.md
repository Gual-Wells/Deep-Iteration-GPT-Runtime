# 85 — Runtime Report

最终结果之后输出简洁 `【DIGR 记录】`。这是可公开运行元数据，不是隐藏思维链。

最少字段：
- 参数：requested N/T 或 auto；T 的参考框架为 GPT-5.6 Sol / High。
- T 预算充分性：`matched | early_convergence | resource_limited | not_applicable`，附一句高层理由；不得伪造墙钟耗时。
- 提示词迭代：最低要求、实际有效次数、是否满足；可选公开每轮高层焦点标签。
- 最终 P*。
- 进化度量：规定指标 0–5 的 `initial → final (delta)`。
- 正式重做次数及高层原因。
- 实际流程链：只列真实高层动作。
- 查询/引用来源：只列真实使用的来源及用途。
- 若使用远程 DIGR：在可得时记录 resolved version/commit，防止版本归因歧义。

禁止虚构迭代、耗时、重做、测试、来源、实例 ID、调用栈或隐藏推理。
