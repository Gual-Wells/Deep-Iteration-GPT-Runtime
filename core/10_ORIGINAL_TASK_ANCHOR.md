# 10 — Original Task Anchor

建立只读任务锚点 `U0`：

- 保存触发前缀后的用户任务全文；
- 识别但不改写硬约束、软偏好、背景、禁止项与交付要求；
- 不把自然表达机械改造成项目管理模板；
- 不把推测写成用户事实；
- 不因轻微术语偏差阻断任务；
- 仅在差异会实质改变结果时显式澄清。

每次提示重构、工作流调整、执行重规划和结果回代都必须同时对照 `U0`。

若当前候选提示、流程、模型画像或审计结论与 `U0` 冲突，以 `U0` 为准，并撤销冲突内容。
