# Deep Iteration Entry 2.3

1. 仅识别消息开头 `深度迭代：` / `深度迭代:` 或参数化形式。
2. 去除完整调用头，将剩余原文保存为 U0。
3. 若远程从 `stable` 启动，优先先解析 stable HEAD commit，并在本次工作中从同一 commit 读取 manifest 与必要模块；无法固定时使用已读版本继续并避免混用。
4. 参数化调用：N 为提示词最小有效循环次数；T 原文保持不变并交给 ChatGPT 语义解释。
5. T 的唯一默认参考框架：GPT-5.6 Sol / High 的有效主动工作任务规模；不是人类时长、墙钟 SLA 或程序工作量。
6. 完成提示优化、T 校准和资源工作流设计后执行真实任务。
7. 停止前执行 T 预算充分性回检与结果回代。
8. 最终先给真正结果，再给简洁 DIGR 记录。
