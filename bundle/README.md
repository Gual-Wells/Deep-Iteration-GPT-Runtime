# Legacy bundle location

Berta2 generates the authoritative execution transport at `dist/EXECUTION_PROTOCOL.json`, as declared by `runtime-descriptor.json`. This directory remains only as a migration signpost; it contains no duplicate protocol bundle.
