# Local Fallback Core — 2.2.0

远程 manifest/模块不可读取时：

1. 仍仅由明确“深度迭代”调用触发；支持可选 `(N,T)` / `（N，T）`。
2. 去除调用头后锚定 U0。
3. 保留三目标：提示优化、GPT 缺陷消融、资源感知工作流进化。
4. 若给 N，至少完成 N 次真实有效的提示词检查循环，不允许表面改写凑数；无法满足则如实报告。
5. T 只作参考复杂度预算，不表示真实等待或墙钟耗时。
6. 完整使用原生能力并按信息增益决定资源/工具。
7. 第一版结果回代；结构性失败才正式重做并计数。
8. 最终先给任务结果，再给 DIGR 记录：参数、实际迭代、P*、0–5 进化度量、重做次数、真实流程链、真实查询/来源。
9. 不输出旧式【执行证据】，不泄露隐藏推理，不虚构动作。
