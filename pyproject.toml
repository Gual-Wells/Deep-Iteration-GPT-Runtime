[project]
name = "deep-iteration-gpt-runtime"
version = "5.0.0-alpha.9"
description = "A native-intelligence assist runtime with liveness-converged repository routing, pre-genesis execution readiness, multi-epoch timing, explicit state memory, and audit support."
requires-python = ">=3.10"
license = "MIT"
