[project]
name = "deep-iteration-gpt-runtime"
version = "3.0.0"
description = "A lightweight result-sovereign evolution protocol for ChatGPT-native task execution."
requires-python = ">=3.10"
license = {text = "MIT"}
