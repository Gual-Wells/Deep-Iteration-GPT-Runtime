[project]
name = "deep-iteration-gpt-runtime"
version = "5.0.0-alpha.10"
description = "A native-intelligence assist runtime with liveness-contracted startup, fast resume, coarse-grained persistence, multi-epoch timing, and audit support."
requires-python = ">=3.10"
license = "MIT"
