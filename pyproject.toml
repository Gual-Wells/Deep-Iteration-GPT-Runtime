[project]
name = "deep-iteration-gpt-runtime"
version = "5.0.0-alpha.4"
description = "A native-intelligence assist runtime with transport-hardened repository routing, atomic clock genesis, explicit state memory, and audit support."
requires-python = ">=3.10"
license = "MIT"
