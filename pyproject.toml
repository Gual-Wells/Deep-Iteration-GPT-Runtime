[project]
name = "deep-iteration-gpt-runtime"
version = "4.1.0"
description = "A task-commitment and native-execution control protocol with repository-delegated semantic authority."
requires-python = ">=3.10"
license = "MIT"
